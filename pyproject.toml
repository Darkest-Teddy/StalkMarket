[project]
name = "farm-finance-api"
version = "0.3.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "pydantic",
    "numpy",
    "pandas",
    "statsmodels",
    "fredapi",
    "google-genai",
]